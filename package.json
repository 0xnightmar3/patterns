{
  "name": "patterns",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "factory": "npm run build && node ./dist/factory/factory.js",
    "builder": "npm run build && node ./dist/builder/builder.js",
    "strategy": "npm run build && node ./dist/strategy/strategy.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/node": "^25.5.0",
    "dotenv": "^17.3.1"
  }
}
